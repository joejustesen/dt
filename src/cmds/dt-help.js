module.exports = (args) => {
  console.log(`
usage: dt <command> [options]

  help        show this message or help for a command
  info	      show environment and all installed packages
  version     show the version of dt
  install     install a new command package
  uninstall   remove a command package
`);
}
